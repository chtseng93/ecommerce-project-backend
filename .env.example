# Copy to .env and fill in. Loaded via: docker run --env-file .env ...
# (Spring Boot reads these as ${DB_USERNAME} / ${DB_PASSWORD} placeholders.)
DB_USERNAME=root
DB_PASSWORD=root
JWT_SECRET=change-me-to-a-long-random-string-min-32-chars
